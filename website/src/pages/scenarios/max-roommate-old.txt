---
const albums = [
  {
    title: "Max Thorne",
    type: "Character Gallery",
    images: 0,
    slug: "max",
  },
  {
    title: 'Jasper "Jazz" Callahan',
    type: "Character Gallery",
    images: 0,
    slug: "jazz",
  },
  {
    title: "Ethan Parker",
    type: "Character Gallery",
    images: 0,
    slug: "ethan",
  },
  {
    title: "Jason Rivers",
    type: "Character Gallery",
    images: 0,
    slug: "jason",
  },
  {
    title: "Brianna “Bree” Carter",
    type: "Character Gallery",
    images: 0,
    slug: "bree",
  },
  {
    title: "Locations",
    type: "World Gallery",
    images: 0,
    slug: "locations",
  },
  {
    title: "Story Moments",
    type: "Scene Gallery",
    images: 0,
    slug: "story-moments",
  },
];
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width" />
    <title>Max the Roommate that Hates You | Scenario Galleries</title>
  </head>

  <body>
    <header class="site-header">
      <a href="/" class="logo">SCENARIO GALLERIES</a>

      <nav>
        <a href="/">Scenarios</a>
      </nav>
    </header>

    <main>
      <a href="/" class="back-link">← All Scenarios</a>

      <section class="scenario-hero">
        <div class="scenario-cover">
          <span>Scenario cover coming soon</span>
        </div>

        <div class="scenario-info">
          <p class="eyebrow">FICTIONLAB SCENARIO</p>

          <h1>Max the Roommate that Hates You</h1>

          <p class="description">
            Character artwork, locations, visual references, and story moments
            from the scenario.
          </p>

          <div class="stats">
            <span>{albums.length} albums</span>
            <span>•</span>
            <span>0 images</span>
          </div>
        </div>
      </section>

      <section class="albums-section">
        <div class="section-heading">
          <div>
            <p class="eyebrow">BROWSE</p>
            <h2>Gallery Albums</h2>
          </div>

          <span>{albums.length} albums</span>
        </div>

        <div class="album-grid">
          {
            albums.map((album) => (
              <a
                href={`/scenarios/max-roommate/${album.slug}`}
                class="album-card"
              >
                <div class="album-cover">
                  <span>Image coming soon</span>
                </div>

                <div class="album-content">
                  <p class="album-type">{album.type}</p>

                  <h3>{album.title}</h3>

                  <p class="image-count">
                    {album.images} {album.images === 1 ? "image" : "images"}
                  </p>

                  <span class="view-link">View gallery →</span>
                </div>
              </a>
            ))
          }
        </div>
      </section>
    </main>

    <footer>
      <p>Scenario Galleries</p>
    </footer>
  </body>
</html>

<style is:global>
  :root {
    color-scheme: dark;
    font-family:
      Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont,
      "Segoe UI", sans-serif;
    background: #0b0b0c;
    color: #f5f5f5;
  }

  * {
    box-sizing: border-box;
  }

  body {
    margin: 0;
    min-height: 100vh;
    background:
      radial-gradient(
        circle at 70% 0%,
        rgba(255, 255, 255, 0.06),
        transparent 35rem
      ),
      #0b0b0c;
  }

  a {
    color: inherit;
    text-decoration: none;
  }

  .site-header {
    width: min(1200px, calc(100% - 40px));
    margin: 0 auto;
    min-height: 76px;
    display: flex;
    justify-content: space-between;
    align-items: center;
    border-bottom: 1px solid #252527;
  }

  .logo {
    font-size: 0.83rem;
    font-weight: 800;
    letter-spacing: 0.16em;
  }

  nav a {
    color: #9a9a9f;
    font-size: 0.9rem;
  }

  main {
    width: min(1200px, calc(100% - 40px));
    margin: 0 auto;
    padding-top: 42px;
  }

  .back-link {
    display: inline-block;
    margin-bottom: 34px;
    color: #909095;
    font-size: 0.9rem;
  }

  .back-link:hover {
    color: white;
  }

  .scenario-hero {
    display: grid;
    grid-template-columns: minmax(280px, 480px) 1fr;
    gap: 64px;
    align-items: center;
    padding-bottom: 90px;
  }

  .scenario-cover {
    aspect-ratio: 4 / 3;
    display: flex;
    align-items: center;
    justify-content: center;
    border: 1px solid #29292c;
    border-radius: 18px;
    background:
      linear-gradient(
        145deg,
        rgba(255, 255, 255, 0.05),
        rgba(255, 255, 255, 0.01)
      ),
      #151517;
    color: #66666b;
    font-size: 0.85rem;
  }

  .scenario-info {
    max-width: 630px;
  }

  .eyebrow {
    margin: 0 0 14px;
    color: #7c7c81;
    font-size: 0.7rem;
    font-weight: 750;
    letter-spacing: 0.18em;
  }

  h1 {
    margin: 0;
    font-size: clamp(2.7rem, 6vw, 5.2rem);
    line-height: 0.98;
    letter-spacing: -0.05em;
  }

  .description {
    max-width: 570px;
    margin: 28px 0 22px;
    color: #a0a0a5;
    font-size: 1rem;
    line-height: 1.7;
  }

  .stats {
    display: flex;
    gap: 10px;
    color: #707075;
    font-size: 0.84rem;
  }

  .albums-section {
    padding-bottom: 110px;
  }

  .section-heading {
    display: flex;
    justify-content: space-between;
    align-items: end;
    padding-bottom: 18px;
    margin-bottom: 28px;
    border-bottom: 1px solid #252527;
  }

  .section-heading h2 {
    margin: 0;
    font-size: 1.55rem;
  }

  .section-heading > span {
    color: #747479;
    font-size: 0.85rem;
  }

  .album-grid {
    display: grid;
    grid-template-columns: repeat(3, minmax(0, 1fr));
    gap: 24px;
  }

  .album-card {
    overflow: hidden;
    border: 1px solid #28282b;
    border-radius: 16px;
    background: #111113;
    transition:
      transform 0.2s ease,
      border-color 0.2s ease;
  }

  .album-card:hover {
    transform: translateY(-4px);
    border-color: #55555b;
  }

  .album-cover {
    aspect-ratio: 4 / 5;
    display: flex;
    align-items: center;
    justify-content: center;
    background:
      linear-gradient(
        145deg,
        rgba(255, 255, 255, 0.04),
        rgba(255, 255, 255, 0.01)
      ),
      #171719;
    border-bottom: 1px solid #28282b;
    color: #626267;
    font-size: 0.8rem;
  }

  .album-content {
    padding: 20px;
  }

  .album-type {
    margin: 0 0 8px;
    color: #727277;
    font-size: 0.68rem;
    font-weight: 750;
    letter-spacing: 0.12em;
    text-transform: uppercase;
  }

  .album-content h3 {
    margin: 0;
    font-size: 1.15rem;
  }

  .image-count {
    margin: 9px 0 20px;
    color: #77777c;
    font-size: 0.82rem;
  }

  .view-link {
    font-size: 0.87rem;
    font-weight: 650;
  }

  footer {
    width: min(1200px, calc(100% - 40px));
    margin: 0 auto;
    padding: 28px 0 50px;
    border-top: 1px solid #252527;
    color: #68686d;
    font-size: 0.8rem;
  }

  @media (max-width: 850px) {
    .scenario-hero {
      grid-template-columns: 1fr;
      gap: 36px;
    }

    .scenario-cover {
      max-width: 550px;
    }

    .album-grid {
      grid-template-columns: repeat(2, minmax(0, 1fr));
    }
  }

  @media (max-width: 600px) {
    .site-header,
    main,
    footer {
      width: min(100% - 28px, 1200px);
    }

    .site-header {
      min-height: 66px;
    }

    main {
      padding-top: 28px;
    }

    .scenario-hero {
      padding-bottom: 65px;
    }

    .album-grid {
      grid-template-columns: 1fr;
    }
  }
</style>