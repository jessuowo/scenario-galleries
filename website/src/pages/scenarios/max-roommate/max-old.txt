---
const images = [
  {
    src: "/images/max-roommate/max/max-01.png",
    alt: "Max Thorne",
  },
  {
    src: "/images/max-roommate/max/max-02.png",
    alt: "Max Thorne",
  },
  {
    src: "/images/max-roommate/max/max-03.png",
    alt: "Max Thorne",
  },
];
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width" />

    <meta
      name="description"
      content="Character gallery for Max Thorne."
    />

    <title>Max Thorne | Scenario Galleries</title>
  </head>

  <body>
    <header class="site-header">
      <a href="/" class="logo">
        SCENARIO GALLERIES
      </a>

      <nav>
        <a href="/scenarios/max-roommate">
          Scenario
        </a>
      </nav>
    </header>

    <main>
      <div class="breadcrumbs">
        <a href="/">Scenarios</a>

        <span>/</span>

        <a href="/scenarios/max-roommate">
          Max the Roommate that Hates You
        </a>

        <span>/</span>

        <span>Max Thorne</span>
      </div>

      <section class="gallery-header">
        <div>
          <p class="eyebrow">
            CHARACTER GALLERY
          </p>

          <h1>Max Thorne</h1>

          <p class="description">
            Character artwork and visual references
            for Max Thorne.
          </p>
        </div>

        <div class="gallery-count">
          {images.length}

          <span>
            {images.length === 1 ? "image" : "images"}
          </span>
        </div>
      </section>

      <section class="gallery">
        {
          images.map((image, index) => (
            <button
              class="gallery-item"
              type="button"
              data-index={index}
              data-src={image.src}
              data-alt={image.alt}
              aria-label={`Open image ${index + 1}`}
            >
              <img
                src={image.src}
                alt={image.alt}
                loading="lazy"
              />
            </button>
          ))
        }
      </section>
    </main>

    <div
      class="lightbox"
      id="lightbox"
      aria-hidden="true"
    >
      <button
        class="close-button"
        id="close-lightbox"
        type="button"
        aria-label="Close image"
      >
        ×
      </button>

      <button
        class="arrow arrow-left"
        id="previous-image"
        type="button"
        aria-label="Previous image"
      >
        ‹
      </button>

      <div class="lightbox-content">
        <img
          id="lightbox-image"
          src=""
          alt=""
        />

        <p id="lightbox-counter"></p>
      </div>

      <button
        class="arrow arrow-right"
        id="next-image"
        type="button"
        aria-label="Next image"
      >
        ›
      </button>
    </div>

    <footer>
      <p>Scenario Galleries</p>
    </footer>

    <script>
      const galleryItems = Array.from(
        document.querySelectorAll(".gallery-item")
      );

      const lightbox =
        document.getElementById("lightbox");

      const lightboxImage =
        document.getElementById("lightbox-image");

      const lightboxCounter =
        document.getElementById("lightbox-counter");

      const closeButton =
        document.getElementById("close-lightbox");

      const previousButton =
        document.getElementById("previous-image");

      const nextButton =
        document.getElementById("next-image");

      let currentIndex = 0;

      function showImage(index) {
        const item = galleryItems[index];

        if (!item) return;

        currentIndex = index;

        lightboxImage.src =
          item.dataset.src || "";

        lightboxImage.alt =
          item.dataset.alt || "Gallery image";

        lightboxCounter.textContent =
          `${currentIndex + 1} / ${galleryItems.length}`;
      }

      function openLightbox(index) {
        showImage(index);

        lightbox.classList.add("open");
        lightbox.setAttribute(
          "aria-hidden",
          "false"
        );

        document.body.classList.add(
          "no-scroll"
        );
      }

      function closeLightbox() {
        lightbox.classList.remove("open");
        lightbox.setAttribute(
          "aria-hidden",
          "true"
        );

        document.body.classList.remove(
          "no-scroll"
        );
      }

      function nextImage() {
        const next =
          (currentIndex + 1) %
          galleryItems.length;

        showImage(next);
      }

      function previousImage() {
        const previous =
          (currentIndex - 1 +
            galleryItems.length) %
          galleryItems.length;

        showImage(previous);
      }

      galleryItems.forEach(
        (item, index) => {
          item.addEventListener(
            "click",
            () => openLightbox(index)
          );
        }
      );

      closeButton?.addEventListener(
        "click",
        closeLightbox
      );

      previousButton?.addEventListener(
        "click",
        previousImage
      );

      nextButton?.addEventListener(
        "click",
        nextImage
      );

      lightbox?.addEventListener(
        "click",
        (event) => {
          if (event.target === lightbox) {
            closeLightbox();
          }
        }
      );

      document.addEventListener(
        "keydown",
        (event) => {
          if (
            !lightbox?.classList.contains(
              "open"
            )
          ) {
            return;
          }

          if (event.key === "Escape") {
            closeLightbox();
          }

          if (event.key === "ArrowRight") {
            nextImage();
          }

          if (event.key === "ArrowLeft") {
            previousImage();
          }
        }
      );
    </script>
  </body>
</html>

<style is:global>
  :root {
    color-scheme: dark;

    font-family:
      Inter,
      ui-sans-serif,
      system-ui,
      -apple-system,
      BlinkMacSystemFont,
      "Segoe UI",
      sans-serif;

    background: #0b0b0c;
    color: #f5f5f5;
  }

  * {
    box-sizing: border-box;
  }

  html {
    background: #0b0b0c;
  }

  body {
    margin: 0;
    min-height: 100vh;

    background:
      radial-gradient(
        circle at 50% -10%,
        rgba(255, 255, 255, 0.055),
        transparent 35rem
      ),
      #0b0b0c;
  }

  body.no-scroll {
    overflow: hidden;
  }

  a {
    color: inherit;
    text-decoration: none;
  }

  button {
    font: inherit;
  }

  .site-header {
    width: min(
      1400px,
      calc(100% - 40px)
    );

    min-height: 76px;

    margin: 0 auto;

    display: flex;
    align-items: center;
    justify-content: space-between;

    border-bottom:
      1px solid #252527;
  }

  .logo {
    font-size: 0.83rem;
    font-weight: 800;
    letter-spacing: 0.16em;
  }

  nav a {
    color: #99999e;
    font-size: 0.9rem;

    transition: color 0.2s ease;
  }

  nav a:hover {
    color: white;
  }

  main {
    width: min(
      1400px,
      calc(100% - 40px)
    );

    margin: 0 auto;
  }

  .breadcrumbs {
    display: flex;
    flex-wrap: wrap;
    gap: 9px;

    padding-top: 36px;

    color: #717176;
    font-size: 0.8rem;
  }

  .breadcrumbs a {
    transition: color 0.2s ease;
  }

  .breadcrumbs a:hover {
    color: white;
  }

  .gallery-header {
    display: flex;
    align-items: flex-end;
    justify-content: space-between;

    gap: 40px;

    padding: 78px 0 58px;
  }

  .eyebrow {
    margin: 0 0 14px;

    color: #77777d;

    font-size: 0.7rem;
    font-weight: 750;

    letter-spacing: 0.18em;
  }

  h1 {
    margin: 0;

    font-size:
      clamp(3.2rem, 8vw, 6.5rem);

    line-height: 0.94;

    letter-spacing: -0.055em;
  }

  .description {
    max-width: 550px;

    margin: 26px 0 0;

    color: #99999e;

    font-size: 0.96rem;
    line-height: 1.65;
  }

  .gallery-count {
    min-width: 110px;

    color: #ededed;

    font-size: 2.7rem;
    font-weight: 700;

    text-align: right;

    line-height: 1;
  }

  .gallery-count span {
    display: block;

    margin-top: 9px;

    color: #6f6f74;

    font-size: 0.68rem;
    font-weight: 600;

    text-transform: uppercase;
    letter-spacing: 0.14em;
  }

  .gallery {
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  gap: 6px;
  padding-bottom: 110px;
}

 .gallery-item {
  display: block;
  width: 100%;
  aspect-ratio: 1 / 1;
  padding: 0;
  overflow: hidden;
  border: 0;
  background: #161618;
  cursor: pointer;
}

 .gallery-item img {
  display: block;
  width: 100%;
  height: 100%;
  object-fit: cover;

  transition:
    transform 0.2s ease,
    opacity 0.2s ease;
}

  .gallery-item:hover img {
    transform: scale(1.018);
    opacity: 0.88;
  }

  .lightbox {
    position: fixed;
    inset: 0;
    z-index: 9999;

    display: none;

    width: 100vw;
    height: 100vh;

    background: #000;

    align-items: center;
    justify-content: center;
  }

  .lightbox.open {
    display: flex;
  }

  .lightbox-content {
    width: 100%;
    height: 100%;

    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;

    padding: 55px 60px 40px;
  }

  #lightbox-image {
    display: block;

    max-width: 100%;
    max-height: calc(100vh - 100px);

    width: auto;
    height: auto;

    object-fit: contain;
  }

  #lightbox-counter {
    margin: 14px 0 0;

    color: #8a8a8f;

    font-size: 0.78rem;

    letter-spacing: 0.08em;
  }

  .close-button {
    position: fixed;
    top: 16px;
    right: 20px;
    z-index: 10001;

    width: 48px;
    height: 48px;

    border: none;
    background: transparent;

    color: white;
    font-size: 2rem;

    cursor: pointer;
  }

  .arrow {
    position: fixed;
    top: 50%;
    transform: translateY(-50%);
    z-index: 10001;

    width: 60px;
    height: 100px;

    border: none;
    background: transparent;

    color: rgba(255, 255, 255, 0.7);
    font-size: 3rem;

    cursor: pointer;
  }

  .arrow:hover {
    color: white;
  }

  .arrow-left {
    left: 10px;
  }

  .arrow-right {
    right: 10px;
  }

  footer {
    width: min(
      1400px,
      calc(100% - 40px)
    );

    margin: 0 auto;

    padding: 28px 0 50px;

    border-top:
      1px solid #252527;

    color: #68686d;

    font-size: 0.8rem;
  }

  @media (max-width: 800px) {
    .gallery-header {
      display: block;

      padding:
        60px 0 44px;
    }

    .gallery-count {
      margin-top: 32px;

      text-align: left;
    }
  }

  @media (max-width: 600px) {
    .site-header,
    main,
    footer {
      width:
        calc(100% - 28px);
    }

    .site-header {
      min-height: 66px;
    }

    .breadcrumbs {
      padding-top: 25px;

      font-size: 0.72rem;
    }

    .gallery-header {
      padding:
        50px 0 38px;
    }

    h1 {
      font-size:
        clamp(
          3rem,
          15vw,
          5rem
        );
    }

    .gallery {
      grid-template-columns: repeat(3, 1fr);
      gap: 3px;
      padding-bottom: 75px;
    }

    .gallery-item {
      margin-bottom: 8px;

      border-radius: 7px;
    }

    .lightbox {
      padding:
        60px 12px 40px;
    }

    .lightbox-content {
      max-width: 100%;
    }

    #lightbox-image {
      max-height: 80vh;
    }

    .arrow {
      width: 42px;

      font-size: 2.7rem;
    }

    .arrow-left {
      left: 0;
    }

    .arrow-right {
      right: 0;
    }

    .close-button {
      top: 8px;
      right: 8px;
    }
  }
</style>